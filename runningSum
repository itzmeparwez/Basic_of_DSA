class Solution {
    public int[] runningSum(int[] nums) {
num[]= {1,2,3,4}
        int res=0;
        for(int i=0;i<nums.length;i++){
            res=res+nums[i];
            nums[i]=res;
            
        }
        return nums;
    }
}

// output [ 1,3,6,10 ]
