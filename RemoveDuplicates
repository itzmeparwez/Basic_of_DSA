class Solution {
    public int removeDuplicates(int[] nums) {  
      nums[] = { 1,1,2,,3,2,1};
       int i=0;
       for(int j=0;j<nums.length;j++){
        if(nums[i]!=nums[j]){
            i++;
            nums[i] = nums[j];
        }  
       }
       return i+1;
    }
}
     